# CentOS 7.1

## Setup server

* Add user for work
    * useradd -m username
    * passwd username
* Add user authority(sudo)
    * visudo
* SSH setup
    * vim /etc/ssh/sshd_config
    * 鍵, noroot, config
    * 
